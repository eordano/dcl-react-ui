// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { Network } from "./Network";

/**
 * The ITEM-unified feed row: one entry per item (not per listing). Same shape as a
 * [`UnifiedListing`] (the surviving representative listing — primary if present, else cheapest
 * credit-buyable secondary) plus a `listingCount` so the frontend can badge "N listings" / link
 * to the PDP resale column. `tradeId`/`listingType`/`tokenId`/`priceCredits`/`source` all
 * describe that representative (headline) listing.
 */
export type UnifiedItem = { source: "native" | "legacy", tradeId: string, listingType: string, contractAddress: string, itemId: string | null, tokenId: string | null, name: string, thumbnail: string, rarity: string, category: string, wearableCategory: string | null, gender: "male" | "female" | "unisex" | null, creator: string, 
/**
 * Representative listing's seller: populated when the headline listing is a secondary
 * (resale), null when it's a primary (mint).
 */
seller: string | null, 
/**
 * Representative listing's NFT mint index (issued id); null for a primary headline.
 */
issuedId: string | null, priceCredits: number, manaWei: string | null, 
/**
 * How many open credit-buyable listings this item has (primary + secondary, native + legacy).
 */
listingCount: number, available: number, network: Network, chainId: number, createdAt: number, };
